<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Campfire" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/dark_colorPrimary</item>
        <item name="colorPrimaryDark">@color/dark_colorPrimaryDark</item>
        <item name="colorAccent">@color/dark_colorAccent</item>
        <item name="android:navigationBarColor">@color/dark_colorAccent</item>
        <item name="android:windowBackground">@color/off_black</item>
    </style>

    <style name="OrangeButtons">
        <item name="android:background">@drawable/lighter_button_shape_dark</item>
    </style>

    <style name="RedButtons">
        <item name="android:background">@drawable/darker_button_shape_dark</item>
    </style>

    <!-- themes with action bar -->
    <style name="Theme.ActionBarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/dark_colorPrimary</item>
        <item name="colorPrimaryDark">@color/dark_colorPrimaryDark</item>
        <item name="colorAccent">@color/dark_colorAccent</item>
        <item name="android:navigationBarColor">@color/dark_colorAccent</item>
        <item name="android:windowBackground">@color/off_black</item>
    </style>

    <!-- themes for chat card view -->
    <style name="ChatCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/dark_chatColor</item>
        <item name="cardCornerRadius">10sp</item>
    </style>

    <!-- chat window nameplate -->
    <style name="ChatWindowNameplate">
        <item name="android:background">@color/dark_colorPrimary</item>
    </style>

    <!-- chat box theme -->
    <style name="ChatBoxTheme">
        <item name="android:background">@drawable/chat_box_theme_dark</item>
    </style>

</resources>